meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: start.py
  persistenceMount: /data
  containerPort: 8000
